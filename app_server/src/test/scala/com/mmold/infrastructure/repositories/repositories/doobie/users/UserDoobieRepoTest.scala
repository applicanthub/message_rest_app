package com.mmold.infrastructure.repositories.repositories.doobie.users

import org.scalatest.{ FlatSpec, Matchers }

class UserDoobieRepoTest extends FlatSpec with Matchers {

  behavior of "User doobie repo test"

  it should "Should accurately verify good ids" in {
    /*
    val user = UserRepositoryDoobieFInterpreter.apply(doobieTranscation).getUserById(UserId("47d0a588-56df-49ce-8a7a-fdcc9ffdafc1")).unsafeRunSync
    println(user.get)
    for (command <- pairsOfValidCredentials) {
      userFactoryInterpreter.create(command).toEither should be('right)
    }*/
  }

}
